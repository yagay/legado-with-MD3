# Uploaded source rebuild

This branch rebuilds custom discovery/settings features from the user-provided source on top of the clean upstream main branch. Existing upstream files are kept wherever possible; custom files are restored directly and upstream APIs receive only compatibility hooks when required.
