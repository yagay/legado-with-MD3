name: Apply Source Kind Reuse Patch

on:
  push:
    branches: [ beta ]
    paths:
      - '.github/workflows/apply-reuse-source-kind-rows.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: beta
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - name: Apply patch
        run: python scripts/apply_reuse_source_kind_rows.py
      - name: Commit patch and remove temporary files
        run: |
          rm scripts/apply_reuse_source_kind_rows.py
          rm .github/workflows/apply-reuse-source-kind-rows.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'perf(explore): reuse prepared source category rows'
          git push origin HEAD:beta
